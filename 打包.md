# 相图学习助手开发与打包说明

本文件夹是给开发者继续修改项目用的源码包。请在这个目录内修改源码，然后重新构建评审版发布包。

## 一、源码包包含什么

主要包含：

```text
src/                  前端 React + TypeScript 源码
backend/              FastAPI 后端源码
assets/               项目图片、架构图等素材
knowledge_docs/       知识库源文档
package.json          前端依赖与脚本
package-lock.json     前端依赖锁定文件
backend/requirements.txt
start.bat             评审版启动脚本
dev.bat               开发模式启动脚本
prepare_runtime.bat   准备内置 Python 运行时
build_release.bat     生成评审版文件夹
get-pip.py            给 embeddable Python 安装 pip
```

源码包默认不包含：

```text
node_modules/         前端依赖，需执行 npm install
.npm-cache/           npm 缓存
backend/.venv/        后端开发虚拟环境
backend/storage/      本地会话、学习记录、ChromaDB 数据
release/              构建产物
runtime/python/       内置 Python 运行时，需自行准备
```

本开发者包已包含：

```text
backend/.env          Qwen 与 GLM 的 API Key 配置
```

## 二、需要安装的软件

开发者电脑需要安装：

```text
Node.js 18 或更高版本
Python 3.11 推荐
```

如果只想生成“解压即用”的评审包，还需要下载 Windows embeddable Python，并解压到：

```text
runtime/python/
```

解压后应存在：

```text
runtime/python/python.exe
```

## 三、首次安装依赖

在项目根目录执行：

```powershell
npm install
```

后端开发环境建议创建虚拟环境：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
cd ..
```

## 四、配置模型 API

开发者包内已提供：

```text
backend/.env
```

其中包含 Qwen 和 GLM 两套 API Key。默认使用 GLM：

```env
LLM_PROVIDER=glm
GLM_CHAT_MODEL=glm-5.1
GLM_VL_MODEL=glm-5v-turbo
```

如果要切换到 Qwen，可改为：

```env
LLM_PROVIDER=qwen
```

注意：`backend/.env` 包含真实 key，只发送给可信开发者，不要上传到公开仓库。

## 五、开发模式运行

开发时使用两个服务：后端 API + Vite 前端。

直接运行：

```powershell
.\dev.bat
```

或手动启动：

```powershell
cd backend
.\.venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8001
```

另开一个终端：

```powershell
npm run dev
```

浏览器访问：

```text
http://127.0.0.1:5173
```

## 六、构建前端生产文件

修改前端或后端后，生成评审版前先构建前端：

```powershell
npm run build
```

成功后会生成：

```text
dist/
```

生产评审版由 FastAPI 直接托管 `dist/`，所以评审版不需要单独启动 Vite。

## 七、准备内置 Python 运行时

为了让评审包在别人 Windows 上“解压即用”，需要准备：

```text
runtime/python/python.exe
```

步骤：

1. 下载 Windows embeddable Python zip。
2. 解压到 `runtime/python/`。
3. 确认 `runtime/python/python.exe` 存在。
4. 在项目根目录执行：

```powershell
.\prepare_runtime.bat
```

该脚本会启用 embedded Python 的 `site`，安装 pip，并把 `backend/requirements.txt` 中的后端依赖安装进 `runtime/python`。

## 八、生成评审版发布包

确认以下步骤已完成：

```text
1. npm install 已完成
2. backend/.env 已配置 GLM API Key
3. runtime/python/python.exe 已存在
4. prepare_runtime.bat 已执行成功
```

然后执行：

```powershell
.\build_release.bat
```

成功后生成：

```text
release/phase-diagram-assistant-review/
```

这个文件夹就是发给用户的评审版运行包。用户解压后双击：

```text
start.bat
```

浏览器会打开：

```text
http://127.0.0.1:8001
```

## 九、发布前自测

进入发布包目录：

```powershell
cd release\phase-diagram-assistant-review
.\start.bat
```

检查：

```text
1. 页面能正常打开
2. 能发送消息并收到模型回复
3. 出题、知识库、学习总结等主要功能可用
4. 关闭 start.bat 终端后，网页刷新会断开
```

## 十、修改项目后的正确流程

不要直接修改 `release/phase-diagram-assistant-review` 里的源码。正确做法：

```text
在源码目录修改 → npm run build → build_release.bat → 使用新的 release 包
```

只有临时替换 API Key 时，可以直接改发布包里的：

```text
backend/.env
```
